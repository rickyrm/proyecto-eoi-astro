<script>
  document.querySelectorAll(".button-contact").forEach(button => {
    button.addEventListener("click", () => {
      window.location.href = "/contact";
    });
  });
</script>

<section class="contact">
  <div class="card">
    <h1>¡Contacta con nosotros!</h1>
    <h3>¿Tienes algún problema?</h3>
    <p>Puedes contactarnos a través del siguiente formulario</p>
    <button class="button-contact" id="contact">Contacto</button>
  </div>
</section>
<p></p>

  <style>
@import url("https://fonts.googleapis.com/css2?family=Questrial&display=swap");

:root {
  --azuloscuropastel: #9abfcf; 
  --azulclaropastel: #afced1;
  --verdepastel: #6cc099;
  --amarillopastel:  #ffdc9c;
  --rosapastel: #ffb6ad;
  --rojopastel: #ee6f6f; 
  --grispastel: #e5decf;
  --grisoscuropastel: #afb7bb;
  --grisclaropastel: #e8e8e8;
  --bordemaqueta: 2px solid black; 
}

.contact{
  color: var(--rojopastel);
  background: linear-gradient(to left, var(--azulclaropastel), white);
  height: 300px;
  width: 100%;
  display: flex; 
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.card {
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 80%;
  width: 50%;
  border-radius: 12px;
  background-color: white;
  /* Añadimos sombra para el efecto 3D */
  box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1), 0 4px 10px rgba(0, 0, 0, 0.1);
  /* Añadimos una ligera transformación para hacer que parezca flotante */
  transform: translateY(-4px);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

/* Efecto cuando se pasa el ratón por encima (hover) */
.card:hover {
  transform: translateY(-8px); /* Hace que la tarjeta suba un poco más */
  box-shadow: 0 12px 20px rgba(0, 0, 0, 0.2), 0 6px 15px rgba(0, 0, 0, 0.15); /* Sombra más intensa */
}

.contact .button-contact {
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  background-color: var(--rojopastel);
  color: white;
  border: none;
  border-radius: 12px;
  height: 56px;
  width: 156px;
  font-size: 16px;
  font-weight: bold;
  cursor: pointer;
  padding: 0;
  box-sizing: border-box; /* Evita que el padding afecte las dimensiones */
  line-height: normal; /* Evita conflictos con el alineamiento vertical */
  box-shadow: 0 1px 3px black;
}

.button-contact:hover {
  background-color: white;
  color: var(--rojopastel);
  border: 2px solid var(--rojopastel);
}

  </style>